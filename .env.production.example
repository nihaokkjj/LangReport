# Copy to .env.production on the ECS host and replace every placeholder.
# Do not commit .env.production.

NODE_ENV=production

POSTGRES_USER=langreport
POSTGRES_PASSWORD=replace-with-a-long-random-password
POSTGRES_DB=langreport

S3_ACCESS_KEY=langreport
S3_SECRET_KEY=replace-with-another-long-random-password
S3_BUCKET=langreport

# The exact public Vercel origin, for example https://langreport.vercel.app
WEB_ORIGIN=https://your-project.vercel.app
API_PORT=4000
TRUST_PROXY=true

# The built-in login gateway verifies this deployment account, signs an HS256
# session JWT, and writes it only to the langreport_session HttpOnly cookie.
AUTH_JWT_SECRET=replace-with-at-least-32-random-characters
AUTH_SESSION_COOKIE=langreport_session
AUTH_SESSION_TTL_SECONDS=604800
AUTH_LOGIN_USERNAME=operator
AUTH_LOGIN_USER_ID=operator
# Generate offline with: pnpm auth:hash-password
AUTH_LOGIN_PASSWORD_HASH='scrypt$replace-with-generated-hash'
# Optional claim checks; signing and verification use the same values.
AUTH_JWT_ISSUER=
AUTH_JWT_AUDIENCE=

# Optional polling intervals for the PostgreSQL-backed workers.
GENERATION_POLL_INTERVAL_MS=1000
RENDER_POLL_INTERVAL_MS=1000
GENERATION_STATUS_LONG_POLL=true

# Required in production. The release gate requires a real Bailian call before
# the Compose stack is started; deterministic is for offline/regression only.
GENERATION_MODE=llm
BAILIAN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
BAILIAN_MODEL_ID=qwen-plus
BAILIAN_STRUCTURED_OUTPUT=json_schema
BAILIAN_TEMPERATURE=0.1
# Generation Worker only; never inject this into API or Web.
BAILIAN_API_KEY=replace-with-worker-only-secret
# Required for the Workspace model-settings UI. Inject this same Base64 32-byte
# value into API and Generation Worker only; never inject it into Web.
# MODEL_CREDENTIAL_ENCRYPTION_KEY=replace-with-32-byte-base64-key
